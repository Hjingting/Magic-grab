// // 与window.resize事件相关联的
// import roll from '../util/roll'

// function initFunction() {
//     //一屏滚动 屏幕高度
//     // 停顿滚动
//     // 向左无缝滚动
//     // 初始化抓盒币的展盒
//     for (let i in reward.coinBox.primary) {
//         $('.boxes').append(`<div class="box commonBox">${reward.coinBox.primary[i]}<img src='${coin}' class='coin'>
//         </div>`)
//     }
//     // 展盒向左循环滚动
//     currentRoller = new roll($('.boxes'))
//     currentRoller.rollLeft()
// }

// initFunction()
window.onload = function(){
    console.log(1)
}
